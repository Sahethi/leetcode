# Python Solutions

This directory contains LeetCode solutions implemented in Python.

## Solutions

1. [Two Sum](1_two_sum.py) - Easy

## Running Solutions

To test a solution locally:

```bash
python3 1_two_sum.py
```

## Tips for Python Solutions

- Use type hints for better code clarity
- Follow PEP 8 style guidelines
- Include complexity analysis in docstrings
- Use descriptive variable names

## Common Patterns

- Hash Maps: `dict()` or `{}`
- Arrays: `list` or `[]`
- Sets: `set()` or `{}`
- Heaps: `import heapq`
- Binary Search: `bisect` module
- Collections: `from collections import deque, defaultdict, Counter`
